import React from 'react';

import './style.css';

const Start = () => (
  <div className='start-wrapper'>
    <div className='start-title'>
      Start making better trading decisions with Bitsgap
    </div>
    <button className='start-button'>Sign up now</button>
  </div>
);

export default Start; 